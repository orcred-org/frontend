node_modules
.next
.env
.env.local
.env*.local
env.local.template
env.live.template

# stray local folders — not part of the project
/landing/
/dashboard/

# Finder/iCloud duplicates ("file 2.tsx", "main 2"). These have already broken
# git fetch once by landing in .git/refs — never let them into a commit.
* 2.*
* [0-9].tsx

# macOS Finder metadata — regenerates on every folder visit
.DS_Store
**/.DS_Store

# TypeScript incremental build cache — rewritten on every build
tsconfig.tsbuildinfo

# Claude Code worktrees and local session state
.claude/worktrees/
